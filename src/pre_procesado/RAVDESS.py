import random
import librosa
import numpy as np
import pandas as pd
import soundfile as sf
from pathlib import Path
from glob import glob
from tqdm import tqdm
from os.path import join, split



def load_RAVDESS_metadata(params):
    # load metadata
    
    # apply splits / folds / cross-validation
    pass
