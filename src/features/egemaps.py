import logging
import librosa
import opensmile
from os.path import join, isfile, sep
from os import makedirs
from tqdm import tqdm

logger = logging.getLogger(__name__)

def egemaps(metadata, params):
    # instantiate opensmile
    
    # extract features
    
    # apply transformations / save features
    pass